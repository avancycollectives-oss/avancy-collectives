"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function AdminLogin(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const router=useRouter();async function submit(e){e.preventDefault();setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)return setError(d.error||"Login failed.");router.push("/admin");router.refresh()}return <main className="av-login-screen"><div className="av-login-card"><p className="av-label" style={{color:"#c9ff4a"}}>AVANCY COLLECTIVES / ADMIN</p><h1>CONTROL<br/><em style={{color:"#a78bfa",fontStyle:"normal"}}>ROOM.</em></h1><p className="av-muted">Manage the catalogue, images and orders from one place.</p><form onSubmit={submit}><label>EMAIL<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>PASSWORD<input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="av-admin-error">{error}</div>}<button className="av-btn av-btn-accent full">SIGN IN ↗</button></form><a href="/">← BACK TO STORE</a></div></main>}
