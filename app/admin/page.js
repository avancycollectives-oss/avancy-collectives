import {cookies} from 'next/headers';import {redirect} from 'next/navigation';import Link from 'next/link';import {validSession} from '../../lib/auth';import {getProducts,getOrders} from '../../lib/db';import AdminSidebar from '../components/AdminSidebar';import AdminFooter from '../components/AdminFooter';export const dynamic='force-dynamic';export default async function Admin(){const c=await cookies();if(!(await validSession(c.get('avancy_admin')?.value)))redirect('/admin/login');const products=await getProducts(),orders=await getOrders();const revenue=orders.filter(o=>o.paymentStatus==='PAID'||o.paymentStatus==='COD').reduce((s,o)=>s+Number(o.total||0),0);return <main className="admin-shell"><AdminSidebar/><section className="admin-main"><div className="admin-head"><div><span>AVANCY COLLECTIVES / CONTROL ROOM</span><h1>DASHBOARD.</h1></div><Link href="/admin/products/new">+ ADD PRODUCT</Link></div><div className="admin-stats"><div><span>PRODUCTS</span><b>{products.length}</b></div><div><span>ORDERS</span><b>{orders.length}</b></div><div><span>RECORDED SALES</span><b>₹{revenue.toLocaleString('en-IN')}</b></div><div><span>LOW STOCK</span><b>{products.filter(p=>Object.values(p.stock||{}).some(n=>Number(n)<5)).length}</b></div></div><div className="admin-panel"><div className="panel-head"><h2>RECENT ORDERS</h2><Link href="/admin/orders">MANAGE ALL →</Link></div>{orders.slice(0,10).map(o=><div className="admin-order-row" key={o.id}><b>{o.id}</b><span>{o.customer?.name||'Customer'}</span><span>{o.customer?.email||'—'}</span><strong>₹{Number(o.total||0).toLocaleString('en-IN')}</strong><span>{o.paymentStatus}</span><Link href={`/admin/orders/${encodeURIComponent(o.id)}`}>{o.orderStatus} →</Link></div>)}{!orders.length&&<p>No orders yet.</p>}</div><AdminFooter/></section></main>}
