import Link from 'next/link';import ProductArt from './ProductArt';import AddToCart from './AddToCart';export default function ProductCard({product}){return <article className="product-card"><Link href={`/product/${product.id}`}><ProductArt product={product}/><div className="product-meta"><div><b>{product.name}</b><small>{product.fit||'Streetwear'} · {product.color||''}</small></div><strong>₹{Number(product.price||0).toLocaleString('en-IN')}</strong></div></Link><AddToCart product={product} compact/></article>}
