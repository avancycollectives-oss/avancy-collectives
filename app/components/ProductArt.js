export default function ProductArt({product,large=false}){return <div className={large?'product-art product-art-large':'product-art'}>{product?.image?<img src={product.image} alt={product.name||'Avancy product'} loading="eager" decoding="async"/>:<div className="art-fallback"><span>{product?.art||'AVNC'}</span></div>}<span className="art-badge">{product?.category||'AVANCY'}</span></div>}
