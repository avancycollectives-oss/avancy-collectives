import {NextResponse} from 'next/server';import {cookies} from 'next/headers';import {validSession} from '../../../../../lib/auth';import {getOrder,updateOrderStatus} from '../../../../../lib/db';
const allowed=['pending','confirmed','processing','shipped','delivered','cancelled','failed','refunded','new'];
export async function PATCH(req,{params}){const c=await cookies();if(!(await validSession(c.get('avancy_admin')?.value)))return NextResponse.json({error:'Unauthorized'},{status:401});const {id}=await params;const body=await req.json();const status=String(body.orderStatus||'').toLowerCase();if(!allowed.includes(status))return NextResponse.json({error:'Invalid order status.'},{status:400});const order=await updateOrderStatus(id,status.toUpperCase());return order?NextResponse.json({order}):NextResponse.json({error:'Order not found.'},{status:404})}
export async function GET(req,{params}){const c=await cookies();if(!(await validSession(c.get('avancy_admin')?.value)))return NextResponse.json({error:'Unauthorized'},{status:401});const order=await getOrder((await params).id);return order?NextResponse.json({order}):NextResponse.json({error:'Order not found.'},{status:404})}
